make a general list here, 

start using issue tracker
next version will be 0.2.0 

transfere longer tasks into bbit 


log, work through all log and channel usage, check the right one is used, 
unify usage in cli by either pass channel or access (pass channel prefered)

care about rpath handling in whoneed fix lookup to check  pathes,    
 
chachsql , use const char* and constexpr for fixed strings

create more modules, shrink amount of public headers and move more into private stuff 


remove all the std container typedef, use auto where used


merge lddirs with all other pathname stuff to have all on one place

todo, review error.hpp , use new style and check what's needed
check dircontent open if it wouldn't be better to return ture false

version update, since there where to many changes, simply print message that file is old and exit